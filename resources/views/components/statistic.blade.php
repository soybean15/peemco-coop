<div>
    <div class="p-6 transition-transform transform bg-blue-200 shadow-lg rounded-xl hover:scale-105 hover:shadow-2xl">
        <div class="flex items-center justify-between mb-4">
            <h2 class="text-sm font-medium text-gray-700">{{ $title }}</h2>
            <i class="text-gray-500 ri-shopping-cart-line"></i>
        </div>
        <div class="mb-2 text-3xl font-extrabold text-gray-800">{{ $value }}</div>
        <p class="text-sm text-green-500">{{ $subtitle }}</p>
    </div>
</div>
